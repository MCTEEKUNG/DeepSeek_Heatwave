# ข้อมูล netCDF/CSV ขนาดใหญ่ — สร้างใหม่ได้ด้วยสคริปต์ใน scripts/
data/*
!data/provinces.csv

# log การดาวน์โหลด
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
Thumbs.db
desktop.ini

# outputs — model artifacts (regenerable) are excluded; verification results are tracked
outputs/*
!outputs/operational/
outputs/operational/*
!outputs/operational/verification/
# forecast snapshots must persist across CI runs so windows can be scored weeks later
!outputs/operational/forecasts/

# tool artifacts
.playwright-mcp/

# large/local archives + preview captures (regenerable; never commit)
data.zip
preview-tab*.png
.env
